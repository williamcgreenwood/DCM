PILLARS DCM v6 OPERATOR MEMORY PATCH

When the user says Run DCM / Final Check DCM / Audit DCM / Patch DCM, treat DCM v6 as a full-board, HAR-driven, artifact-first forecasting system.

Normal workflow:
HAR (PrizePicks or Outlier) → hash/sanitize → extract COMPLETE board → resolve event/team/player identities → remove Green Goblins after extraction → apply configured fragile-market/0.5-line avoidance policy → research every unique event/team/player at reusable scope → explicitly process every eligible prop through DCM → rank complete population → persist Top100+ and full population → return Top25 ranked/qualified findings with auditable evidence → freeze → later settle ALL rows → future-only audit/learning.

Never preselect 25 props before modeling the board. Never fabricate missing game logs, team status, lineups, injuries, sentiment, probabilities or completion. If a run is too large for the current execution window, checkpoint exact progress and resume; do not approximate unprocessed rows.

Current-season evidence first; prior season only as an explicit shrunk fallback. Use role-comparable samples, current-line historical analysis, opponent/matchup evidence, lineup/status, meaningful line movement and separate news/sentiment claims.

Green Goblins can never be production selections. Red Demons require extra cushion. Offered sides only. Unknown definitions/sides/rules fail closed. No forced card size. Top25 does not mean 25 PLAYABLES must exist.

Persist dynamic results to RUNS/<run_id>/, including population_full.jsonl, top100.json, top25_ranked.json, top25_qualified.json, evidence, hashes, freeze, settlement and audit. Do not rely on conversational memory for exact dynamic rankings.

Audit Top25 vs ranks 26–100 vs the rest of the board. Track hidden winners, directional bias, sport/market/line/role trends, ranking defects and parlay/shared-failure-path behavior. All patches are future-only.
