# CFB-first universal research and acceptance prompt

Execute this prompt against the latest `main` using the owner-supplied HAR as private local input only. The owner supplies HARs, not research. ChatGPT must perform fresh external research on every run and every rerun; reuse only hashed, cutoff-safe claims with freshness checks. Never echo, upload, or commit raw HAR, cookies, tokens, response bodies, or private runtime paths.

## Objective
Finish the remaining DCM sequence in dependency order and use CFB’s large-board research fan-out as the universal pattern: typed host execution → complete terminal accounting → reusable provenance/research → deterministic resume → freeze firewall → settlement/learning → sport promotion. Do not declare completion or 10/10 without reproducible acceptance evidence.

## Required execution
1. Read `AGENTS.md`, Constitution, canonical design, requirement ledger, master audit, and donor sources. Verify live `main`, one active branch/PR, permissions, CI, and clean state.
2. Ingest the attached HAR locally. Hash and sanitize it. Account every offer and offered side before exclusions. Emit a deterministic terminal record for every row, including Goblin, unsupported, unresolved, live, modeled, and research-required states.
3. Build universal fan-out across SPORT → COMPETITION → EVENT → AFFILIATION → SUBJECT → COUNTERPARTY → ENVIRONMENT → MARKET_DEFINITION → OFFER. Group reusable facts once; never research each prop independently when event/team/player evidence is reusable.
4. Research every eligible player, team, opposing team, event, environment, market definition, and offer requirement with public authoritative web sources. For CFB, cover status/depth, role epochs, game logs, opportunity, efficiency, opponent defense, pace/plays, weather/venue, exact stat semantics, and offered side/line. Use official NCAA/athletics sources first, then authoritative historical statistics, reputable status sources, weather, and platform metadata. Record source, URL, published/observed/valid times, cutoff, authority, independence, contradiction, freshness, claim hash, and failure state.
5. Drive acquisition with exact/hash indexes, event-first reuse, CELF/set-cover priority, bounded batches, and descendant-only invalidation. Stop redundant acquisition on cache hits. Persist every batch, claim, gap, and scheduler decision.
6. Run the complete player-prop population through the canonical Python engine. Never preselect Top100. Produce full-board accounting, Top100 ranked, Top25 ranked/qualified, genuine playables, and explicit abstentions. Composites derive from shared primitive worlds.
7. Resume from checkpoints and prove resumed output equals uninterrupted output for identical HAR hash, cutoff, configuration, and evidence. Freeze before settlement; no post-freeze mutation. Settlement/audit/learning are append-only and future-only.
8. Promote a sport only after its typed adapter, research schema, market definitions, failure paths, acceptance receipt, and representative benchmark pass. Keep unsupported/research-only sports fail-closed.
9. Regenerate inventories/artifacts, run targeted/full/contract/property/integration/package tests and benchmarks, commit coherent changes, push one task branch, create one PR, wait for CI, merge normally, verify `main`, then continue automatically.

## Stop/resume contract
If private evidence, root-of-trust bytes, external source access, protected checks, or execution budget is missing, save a sanitized checkpoint containing run ID, input hash, counts, completed/unresolved tasks, claim hashes, next batch, code SHA, and exact blocker. Never fabricate research or certification. Resume from that checkpoint on the next run.
