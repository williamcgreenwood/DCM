# P380X donor quarantine and accounting

This directory contains governance/reference artifacts, not importable DCM runtime modules.

Exact original P380X/GROK ZIP bytes were not available to this execution host. No extraction or archive hash was fabricated. The canonical state for this tranche is `EXACT_ARCHIVE_BYTES_UNAVAILABLE`.

`DONOR_COMPONENT_MATRIX.json` accounts for all 58 principal donor candidates. A disposition is not activation. Production activation still requires a DCM-native contract, normalized inputs and units, a producer, an explicit canonical consumer, tests, and audit lineage.

The active runtime does not load this directory and a wheel does not require donor files.
